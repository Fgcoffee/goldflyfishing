__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
build/dist/
# PyInstaller's working directory (BUILD.md passes --workpath build/work).
build/work/
dist/

# Local data cache: Scryfall bulk dumps and the derived card DB.
# Regenerated by `python -m mtgfish.tools.fetch_scryfall`.
cache/
*.sqlite
*.sqlite-journal
*.jsonl
*.jsonl.gz

# Simulation output
runs/
*.parquet
